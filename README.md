# Acquire Package Boilerplate

To run project locally

```
 yarn
 yarn start
```

To build project

```
 yarn build
```

It compiles into /lib folder

    /lib/bundle [udm]
    /lib/cjs [commonjs]
    /lib/esnext [exnext]
